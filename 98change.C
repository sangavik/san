#include<stdio.h>
int main()
{
int i,n,num;
printf("\n enter the num:");
scanf("%d",&n);
printf("\n enter the numbers with one changed number:");
for(i=1;i=n;i+=)
{
scanf("%d",&num[i]);
}
for(i=1;i<=n;i++)
{
if(i!=num[i])
{printf("%d",i);
} else 
break;
}
return 0;
}
